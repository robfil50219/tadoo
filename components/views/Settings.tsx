/**
 * @deprecated Import Settings from `@/features/settings`.
 * This compatibility entry can be removed after downstream imports migrate.
 */
export { default } from '@/features/settings/components/Settings';
